# Env-specific, NON-secret values only. Credentials live in .env (gitignored).
# NOTE: this practice app only has one real hosted instance — dev/qa/uat all
# point at it for now. The config layer is built for multiple environments
# before the infra for them exists, which is the normal order on a real project.
BASE_URL=https://eventhub.rahulshettyacademy.com
API_BASE_URL=https://api.eventhub.rahulshettyacademy.com/api
